<!-- HEADER  -->
	<body>
		<header class="header">
			<div class="container">

				<div class="cat">
					<div class="row">
						<div class="col-sm-7">
							<%= link_to "N.W?", root_path, class: "btn bt-link logo_header b_header" %>

						</div>		

						<!-- BOTONERA -->

						<div class="col-sm-5 botonera">
						
							<%= link_to "Home", root_path, class: "btn bt-link b_header" %>


							<%= link_to "Categories", categories_path, class: "btn bt-link b_header" %>

								<button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
							  <%= current_user.name %> <span class="caret"></span>
							  </button>

							  <ul class="dropdown-menu" role="menu">
							    <li> <%= link_to( current_user.name, user_path(current_user.id))%> </li>

							    <li class="divider"> </li>
							    <li> <a href="#"> <%= render "users/logout_form" %> </a> </li>
							  </ul>
						</div>
						 

						 <!-- //BOTONERA -->
						
						</div>
						</div>	
					</div>
				</div>
		</header>
	<!-- HEADER -->