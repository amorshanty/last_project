
<div class="container">

	<div class="row">
		<div class="col-sm-8 text-center col-sm-offset-2">
			<% @categories.each do |category| %>
				<%= link_to category.name, category_path(category), class: "btn btn-primary btn_li" %>
				
				<%end%>
		</div>
	</div>
</div>